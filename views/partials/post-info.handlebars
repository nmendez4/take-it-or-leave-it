<article class="post">
  <div class="title">
      <h1>{{product_name}}</h1>
      <p>{{description}}</p>
  </div>
  <div class="meta">
    By {{user.username}} on {{format_date created_at}}
    | {{like_count}} Interested |
    <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
  </div>
</article>